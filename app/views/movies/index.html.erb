<%= link_to 'Back to the lists', lists_path, class: "btn btn-secondary" %>
<br>
<br>
<h1>My Movies</h1>
<br>
<% @movies.each do |movie| %>
  <h5><strong><%= movie.title %> • <%= movie.rating %>/10</strong></h4>
  <p><%= movie.overview %></p>
  <%= link_to "See more", movie_path(movie), class: "btn btn-outline-primary" %>
  <%= link_to 'Edit this movie', edit_movie_path(movie), class: "btn btn-outline-secondary" %>
  <%= link_to "Delete", movie_path(movie), method: :delete, data: { confirm: "Are you sure you want to delete this movie?" }, class: "btn btn-outline-danger" %>

  <br>
  <br>
<% end %>
